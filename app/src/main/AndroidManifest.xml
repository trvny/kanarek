<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- usesCleartextTraffic: IPTV/radio playlists routinely carry plain http:// stream and
         logo URLs; without this, targetSdk 28+ blocks them and those stations silently fail. -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanarek">

        <!-- The one window: reader + player as swipeable pager pages with a nav drawer.
             singleTop so the player widget's deep-link (EXTRA_PAGE) reuses the open window. -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Kanarek"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home-screen widget. Only the system update action is public; custom refresh/control
             PendingIntents target WidgetActionReceiver below, which is deliberately unexported. -->
        <receiver
            android:name=".widget.KanarekWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kanarek_widget_info" />
        </receiver>

        <!-- Private target for Kanarek-created widget PendingIntents. Keeping it unexported prevents
             other apps from sending refresh/playback commands directly. -->
        <receiver
            android:name=".widget.WidgetActionReceiver"
            android:exported="false" />

        <!-- Feeds the slideshow collection -->
        <service
            android:name=".widget.NewsRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- Trampoline: re-dispatches a widget card tap as an explicit ACTION_VIEW so the
             collection click template can be an Android 14+-legal mutable PendingIntent. -->
        <activity
            android:name=".widget.ArticleRedirectActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Radio/IPTV background playback: one ExoPlayer + MediaSession for the whole app,
             so playback and lock-screen/notification controls survive the Activity. Exported
             so the system (media notification, Android Auto, "resume playback") can bind. -->
        <service
            android:name=".player.PlayerService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Home-screen widget: current station + play/pause/next/prev, pushed live by
             PlayerService (updatePeriodMillis=0 — no polling). -->
        <receiver
            android:name=".widget.PlayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info" />
        </receiver>
    </application>
</manifest>

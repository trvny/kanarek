<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanarek">

        <!-- Launcher: a chooser between the two products (News / Radio-TV). -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Kanarek">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Feed management + preview; reached from HomeActivity. Keyboard for feed/backend fields. -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/home_news"
            android:theme="@style/Theme.Kanarek"
            android:windowSoftInputMode="adjustResize" />

        <!-- Radio/IPTV playlist screen: manage stations, import/export M3U, play. -->
        <activity
            android:name=".ui.PlayerActivity"
            android:exported="false"
            android:label="@string/player_title"
            android:theme="@style/Theme.Kanarek"
            android:windowSoftInputMode="adjustResize" />

        <!-- Home-screen widget -->
        <receiver
            android:name=".widget.KanarekWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.kanarek.action.REFRESH" />
                <action android:name="com.kanarek.action.OPEN" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kanarek_widget_info" />
        </receiver>

        <!-- Feeds the slideshow collection -->
        <service
            android:name=".widget.NewsRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!-- Trampoline: re-dispatches a widget card tap as an explicit ACTION_VIEW so the
             collection click template can be an Android 14+-legal mutable PendingIntent. -->
        <activity
            android:name=".widget.ArticleRedirectActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Radio/IPTV background playback: one ExoPlayer + MediaSession for the whole app,
             so playback and lock-screen/notification controls survive the Activity. Exported
             so the system (media notification, Android Auto, "resume playback") can bind. -->
        <service
            android:name=".player.PlayerService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Home-screen widget: current station + play/pause/next/prev, pushed live by
             PlayerService (updatePeriodMillis=0 — no polling). -->
        <receiver
            android:name=".widget.PlayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.kanarek.player.widget.action.TOGGLE" />
                <action android:name="com.kanarek.player.widget.action.NEXT" />
                <action android:name="com.kanarek.player.widget.action.PREV" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/player_widget_info" />
        </receiver>
    </application>
</manifest>
